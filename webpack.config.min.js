var webpack = require('webpack');

module.exports = {
	entry:{
		partone:'./scripts/codeSplitting.js',
		parttwo:'./scripts/entryDemo5.js'
	},
	output:{
		path: __dirname + "/tmp",
		filename:"[name].js",
		publicPath:__dirname+"/tmp/"
	},
	module:{
		loaders:[
			{test:/\.css$/,loader:"style!css"}
		]
	},
	plugins:[
		//生成的 partone.js, parttwo.js, 1.1.js, 2.2.js 会是压缩之后的形式
		new webpack.optimize.UglifyJsPlugin({
			//设置 uglifyJs 的参数。此处以 --compress warnings:true 为例
			compress:{
				warnings:true
			}
		})
	]
};